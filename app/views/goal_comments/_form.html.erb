<h1>Add Comment</h1>
<form action="<%= goal_goal_comments_path(@goal) %>" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <textarea name="goal_comment[body]" id="body"></textarea><br>
  <input type="submit" value="Add Comment">
</form>
